name: Templates

on:
  push:
    branches: [main]
    paths-ignore:
      - LICENSE
      - README.md
  pull_request:
    branches: [main]
    paths-ignore:
      - LICENSE
      - README.md

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    strategy:
      matrix:
        template:
          - templates/default
      fail-fast: false
    runs-on: macos-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: ${{ matrix.template }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: accept-flake-config = true
      - name: Build template
        run: nix run home-manager/master -- build --flake .
